package org.acme.rest;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeGreetingResourceIT extends EnvironmentResourceTest {

    // Execute the same tests but in native mode.
}